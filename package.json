{
  "name": "qunhao",
  "version": "1.0.0",
  "private": true,
  "description": "群号 - 稳定的群二维码链接服务（Cloudflare Pages + Workers）",
  "scripts": {
    "dev": "wrangler pages dev",
    "deploy": "wrangler pages deploy",
    "typecheck": "tsc --noEmit",
    "db:init:local": "wrangler d1 execute qunhao-db --local --file=./schema.sql",
    "db:init:remote": "wrangler d1 execute qunhao-db --remote --file=./schema.sql",
    "db:beijing:local": "wrangler d1 execute qunhao-db --local --file=./schema-beijing-pilot.sql",
    "db:beijing:remote": "wrangler d1 execute qunhao-db --remote --file=./schema-beijing-pilot.sql",
    "seed:beijing:craftsmen": "node scripts/seed-beijing-craftsmen.cjs",
    "seed:beijing:orders:demo": "wrangler d1 execute qunhao-db --local --file=./schema-beijing-orders-demo.sql",
    "test:beijing:smoke": "node scripts/beijing-pilot-smoke.cjs"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260610.0",
    "typescript": "^5.8.0",
    "wrangler": "^4.0.0"
  },
  "dependencies": {
    "jsqr": "^1.4.0",
    "qrcode-generator": "^2.0.4"
  }
}
